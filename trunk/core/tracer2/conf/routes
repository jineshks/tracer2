# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           				controllers.Application.index()

GET    /api/tracer.com/v1/login									controllers.UserController.login()
GET   /api/tracer.com/v1/addProject            				controllers.UserController.addProject()
GET   /api/tracer.com/v1/addUserToProject            		controllers.UserController.addUserToProject()
GET   /api/tracer.com/v1/inviteUser            					controllers.UserController.inviteUser()
GET   /api/tracer.com/v1/registration            				controllers.UserController.registration()
# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
